#include <stdio.h>
#include <stdlib.h>
#include<string.h>
#include<regex.h>
int main()
{
	char file_name[15];
//char *first_name[15];
//char second_name[15];
	char str[200];
	char ch,line[10];
//char temp="import;
	int i=0;
	FILE *fp;
	printf("Enter the filename to be opened \n");
	scanf("%s", file_name);
	fp = fopen(file_name, "r");
	if (fp == NULL)
	{
		printf("file is not present \n");
		exit(0);
	}
	ch=fgetc(fp);
	while(ch!=' ')
	{
		line[i]=ch;
		i++;
		ch = fgetc(fp);
	}
	if(strcmp(line,"import")==0 || strcmp(line,"package")==0)
	{
		printf("this is java file");
	}
	else if(strcmp(line,"<?php")==0)
	{
		printf("this is php file");
	}
	else
	{
		printf("this is ruby file");
	} 
/*	while(ch!=EOF)
	{
		if(ch!=' ')
		{
			cls[i]=ch;
			i++;
			printf("%s",cls);					
		}
		printf("%s",cls);					

		if(strcmp(cls,"class")==0)
		{
			while(ch!=' ');
			{
				putchar(ch);			
			}
		}
		ch=fgetc(fp);	
	}*/
fgets(str, sizeof(str), fp);
if(str=="class")
{
	printf("there is class");
}
	fclose(fp);
	return(0);
}

