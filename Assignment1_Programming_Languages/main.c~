#include<stdio.h>
#include<stdlib.h>
int main()
{
char file_name[20],ch;
FILE *fp;
printf("enter file name =");
scanf("%d",javatest.java);
fp=fopen(File_name,w);
if(fp==NULL)
{
printf("cannot open file");
exit(0);
}
ch = fgetc(fp);

    while (ch != EOF)

    {

        printf ("%c", ch);

        ch = fgetc(fp);

    }
    fclose(fptr);
return(0);
}
